# 0107 — The vertex tier: the vertex is the total correlation

The isomorphism's vertex/joint tier, closed as one theorem verified
in three habitats. Code: `output/0097_the_vertex_tier.py`.

> **A product weight's price is the total correlation it drops, and
> the vertex factor is that correlation.**

- **Exact habitat** (linear-Gaussian): two streams with correlated
  process noise; a product bank of two *correct-marginal* Kalmans vs
  the joint filter. Each marginal Kalman achieves its stream's
  entropy rate and the joint achieves the joint rate, so the product
  bank's excess is exactly the streams' mutual information rate,
  −(1/4π)∫ ln(1 − coh²) dω. Measured 0.03377 vs spectral 0.03370
  nats/step — **ratio 1.002**.
- **Physics habitat** (the derived vertex ensemble, 0090's
  machinery): the vertex weight is non-factorizing across the six
  planes; its Gaussian-sector total correlation is **TC_G = 0.98
  nats per vertex** (block-determinant identity). The identity
  E[ln(joint/Π marginals)] = TC is definitional; TC_G is the
  Gaussian part of the vertex's price, and the non-Gaussian
  remainder is exactly the territory 0089's context spectrum
  measured (+1.17 nats).
- **Field habitat** (recorded): GPB1's collapse relief in the lucid
  repository (ridge 0.0022 → 0.0101 nats/pt under the joint repair)
  — the same theorem paying out on real data.

Consequences for the standing opens: "vertex corrections' effect on
the dressed vacuum" now has a formal frame — the lattice's ledger
tier (product over plaquettes) underprices the measure by the TC of
plaquettes sharing a vertex, so the correction to 0101/0102's
observables is a computable TC insertion, not a new coupling. The
vertex's strength *is* the legs' mutual information.

## Honest limits

- TC_G is the Gaussian sector; the full TC of the 36-dim vertex
  ensemble needs nonparametric entropy estimation (or the cumulant
  expansion 0089 started). The identity is exact; the number is the
  Gaussian part.
- The three habitats share the theorem, not a single normalization:
  0.034 nats/step, 0.98 nats/vertex, 0.008 nats/pt are the same
  object in three different units.

## Open

1. Insert the TC correction into the lattice measure (the vertex
   port, now well-posed) and re-measure the scale-field deficit.
2. The non-Gaussian TC remainder vs 0089's context spectrum,
   cumulant by cumulant.
