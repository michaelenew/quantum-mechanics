# 0105 — The prequential tier: the action is a code length

Third isomorphism stone: the operational loss. The filter's
epistemology rests on prequential code length; the physics' ledger
prices in nats but was scored against nothing. At the exact tier
(2D Z_N, where the budget was derived) the identification is a
theorem. Code: `output/0095_the_prequential_tier.py`.

> **The Euclidean action of a record is the prequential code length
> of revealing it, under the ledger's own predictor — in any order.**

Three exact statements, each verified to machine precision:

1. **Action = code.** Reveal the plaquette record and score each
   reveal: the total is −ln P(config) — the action in nats —
   identically (0.0e+00 spread over reveal orders at P = 12).
2. **No arrow = order-invariance.** The chain rule makes the total
   permutation-invariant: the Euclidean measure's "no time" is the
   code length's indifference to reveal order. What a Lorentzian
   reading adds is exactly *an order* and nothing else — the same
   statement 0103 proved as smoother-vs-filter and 0100 made for the
   causal layer ("the arrival order of boundary data"), now as an
   exact identity of totals.
3. **The boundary carries the budget, in nats.** On the closed
   surface the per-step predictor is a genuine filter (state = the
   running holonomy posterior; each prediction q(m)·C_{k−1}/C_k),
   and knowing the surface is closed saves exactly
   −ln P_free(hol = 0) nats: 1.603367 at P = 8, **ln 5 to six
   digits by P = 64**, at the confinement rate. Gap 2's innovation
   capacity (0100) reappears as an operational loss: the boundary
   holonomy carries exactly the budget's worth of the record's code.

The asymmetry 0079 called the physics' weakest point — "no
operational loss" — is gone at this tier: the ledger *is* scored,
against the record stream, and its score is the filter's.

## Honest limits

- Exact at the 2D Z_N ledger tier (product record law + budget
  constraint), where free/constrained is fully computable. The 4D
  nonabelian version needs the boundary-state vertex — the same
  stone F1 waits on; nothing here shortcuts it.
- The reveal predictor scores the *record* (modulus) ledger; the
  source ledger (phase, 0086) never enters the code — consistent
  with the two-ledger theorem, but the phase's own operational
  meaning (interference utility) is exactly F4's open question.

## Open

1. F4 (coherent bank) is now the sharpest remaining seam: the record
   ledger has an operational loss, the source ledger does not — if
   amplitude-mixing wins on classical data anywhere, that is the
   phase's missing operational meaning.
2. The nonabelian/4D version rides on the boundary-state vertex (F1).
