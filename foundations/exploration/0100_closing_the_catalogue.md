# 0100 — Closing the catalogue: the innovation channel, and the derived vacuum's mixture

The isomorphism-gap catalogue's last two items (0098's gaps 2 and 3),
closed to the extent the local models allow — including the
correspondence arc's one genuine surprise *against* an earlier
expectation, recorded as such. Code:
`output/0090_closing_the_catalogue.py`.

---

## 1. Gap 2 — the innovation channel, formalized and measured

The homomorphism's second failure was stated as an absence: a filter
conditions on external innovations; the physics' cycle only
self-conditions (parallel replicas). The question "what external
stream does the physics admit?" turns out to have an exact,
already-derived answer:

> **The boundary is the one innovation channel, and its capacity is
> the quantized budget.** By Stokes (0080), the boundary holonomy is
> a deterministic function of the bulk, so
> I(bulk; boundary) = H(hol(∂)) — computed exactly from 0080's dual
> formula: H = 1.6065 at P = 4 plaquettes, and **ln N to six digits
> by P = 16**, approached at the confinement rate.

Corollaries, each the formal version of something the program had
been circling: the **closed universe is the innovation-free filter**
(pure self-conditioning — which is why its Euclidean measure has no
time); *who supplies the innovation* = *where is the boundary*;
**measurement = opening a boundary**; and the causal layer — the
wall's one permanent stratum — is the *arrival order* of boundary
data. The measurement thread now has a definition instead of a
mystery: to add observers to this theory is to specify its boundary
traffic.

## 2. Gap 3 — the derived vacuum's mixture: a split verdict

0097 verified the marginalization *cure* on hand-built vacua and
left the derived question open: does the interacting measure itself
produce a scale-mixture vacuum (the physical s_P > 0)? Two local
derived models answer differently:

**Group level (the Born-weight plaquette ensemble): the mixture is
real.** SD(ln θ) = 0.693 against 0.483 for the second-moment-matched
Gaussian radial law — a **+43% radial-mixture excess**, kurtosis 13.
The derived group-level vacuum is a genuine scale mixture.

**Algebra level (Metropolis on the single-site vertex ensemble,
compactness box |F_p| ≤ π): no mixture at all.** Component kurtosis
2.59 — *sub*-Gaussian — and SD(ln ρ) = 0.079, *below* the Gaussian
control's 0.123. The power-law vertex weight is non-normalizable
without the box, so the box sets the scale and the ensemble pins
against it: **cutoff-dominated**, the opposite of heavy-tailed.

So 0097's expectation is confirmed at group level and **refuted at
algebra level** — the arc's one genuine surprise against us, worth
exactly as much as the confirmations. The two local regulators
disagree about the physical s_P; the discriminating computation is
the full lattice Monte Carlo (approved earlier, still the heavy
remainder), which now has a sharp, binary question to answer rather
than an exploratory one.

## 3. The catalogue, closed out

| gap | status |
|---|---|
| 1 — group curvature | **closed**: δ = 1/6 theorem, β = (1−1/b²)τ²/6; the running S³ filter built (0099) |
| 2 — external innovations | **closed**: the boundary is the channel; capacity = quantized budget; closed universe = innovation-free filter (here) |
| 3 — the hypothesis bank | **partial**: cure verified (0097); derived verdict regulator-split (here); discriminator = lattice MC, queued |
| 4 — discrete sectors | **closed as experiment**: regime-hazard priced both ways, sectors are slow observables (lucid 0003) |

## Honest limits

- Gap 2's theorem is at the abelian/2D tier where the budget was
  derived; the 4D nonabelian boundary (the boundary-state vertex,
  still open) is where the full version must live.
- Gap 3b's MC is single-site with a hard spherical box; a soft
  compactification or the true group-level lattice measure could
  move the verdict — that is exactly why it is recorded as
  regulator-split rather than settled.
- "Measurement = opening a boundary" is a formal identification
  within this program's objects, not a claim to have solved the
  measurement problem; it converts the question into a computable
  shape (boundary traffic), which is all this stone claims.

## Open

1. The full lattice MC (gap 3's discriminator — the one approved
   heavy computation still standing).
2. The 4D nonabelian boundary/innovation channel (merges with the
   boundary-state vertex, the program's standing heavy stone).
3. The filter-side δ measurement and the S³ association problem
   (0099's opens, unchanged).
