# 0115 — The bridge closed, which bound binds, and the ratio-record audit

> **AI-generated, not peer-reviewed.** See [`ATTRIBUTION.md`](../ATTRIBUTION.md) — results here are re-derivations of established work unless explicitly
> marked otherwise, and prior art is credited there.

Three residuals, run together. Code:
`output/0105_the_bridge_and_the_bounds.py`.

## 1. The bridge — and it closes

0114 left the first law's requirement α = π against 0082's measured
0.0242 as a "debt for the κ normalization to supply". Reading 0082
properly closes it, in three parts:

- **(a) The requirement was never a coincidence.** The completion's
  own Newton constant is G = 1/4π (since λ = M/4πr *is* Φ = GM/r),
  and S = A/(4G) at that G is **exactly α = π**. So the first law's
  demand is precisely Bekenstein–Hawking: **the completion satisfies
  S = A/4G with the 1/4**, not merely the shape. The program's C4
  target ("the 1/4") is met on this axis.
- **(b) The induced-gravity closure.** 0082's α = 0.0242 is *per
  scalar polarization*; the graviton carries two, so
  α_total = 0.0484. Equating the horizon entropy with the measured
  **vacuum entanglement entropy** — this program's own C4 question —
  fixes the lattice Newton constant: 1/(4G) = α_total ⟹
  **G = 5.17 a², a Planck length of 2.27 lattice spacings.** The
  κ-normalization debt is thereby closed *conditionally on the
  induced-gravity identification*, and it makes a testable
  prediction rather than absorbing a factor.
- **(c) The named structural residue.** The completion is a
  **scalar** theory, so its horizon sits at r_h = GM — half the
  Schwarzschild radius; a factor 2 in radius, 4 in area and
  temperature. Bridge bookkeeping: π/α_scalar = 129.8 = 2
  (polarizations) × 64.9 (the G ratio). Nothing is unaccounted.

## 2. Which bound binds

Two caps on a region's mass — information (each node carries mass
< 1, so M < n_nodes) and geometry (M ≤ capacitance ≈ 4πR):

| R | n_nodes | capacitance | binds |
|---|---|---|---|
| 1.0 | 7 | 12.1 | information |
| 1.5 | 19 | 18.0 | geometry |
| 2.0 | 33 | 23.3 | geometry |
| 4.0 | 257 | 54.6 | geometry |

They cross near R ≈ √3 ≈ 1.7 spacings. **Below ~2 lattice spacings
a body is limited by how much information its nodes can hold; above
it, by its own geometry.** Gravity's bound is geometric for
anything bigger than a couple of Planck lengths — and with (1b)'s
ℓ_P = 2.27 a, the crossover sits right at the Planck scale, as it
should.

## 3. The ratio-record audit — a corrected premise

The derived-gravity chain (lucid 0019) assumes spatial records are
ratio-valued. Audited on this program's own lattice: the site
log-scale field's zero-mode power is 0.0500 against 0.0312 for
non-zero modes — ratio 1.60, i.e. **no soft zero mode**. The derived
measure *pins* the absolute scale, so lattice records are **not**
ratio-valued in the naive sense.

The honest form of the premise is the **deviation** form: records of
departures from the pinned vacuum are difference-valued — which is
exactly how the derivation used it (λ = 0 vacuum, differences only).
A correction to the premise's *statement*, not to its use.

## Honest limits
- (1b) is conditional on identifying horizon entropy with vacuum
  entanglement entropy; that identification is a hypothesis this
  program has flagged since 0082, not a result.
- The scalar/tensor factor in (1c) is named, not derived from a
  tensor completion.
