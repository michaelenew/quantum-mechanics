# 0049 — The closed loop: the string sector joins the conserved-source law

> **AI-generated, not peer-reviewed.** See [`ATTRIBUTION.md`](../ATTRIBUTION.md) — results here are re-derivations of established work unless explicitly
> marked otherwise, and prior art is credited there.

Post-consolidation fronts, two closed. 0047 predicted the wiggling
string's radiative admixture was an open-end artefact and that a
compact, exactly conserved string would radiate clean vacuum waves.
Built and confirmed — and 0048's κ residue closes at its honest
level on the way. Code: `output/0044_the_closed_loop.py`.

---

## 1. The exact loop

A Kibble–Turok Nambu–Goto solution:
x(σ,t) = ½[A(σ−t) + B(σ+t)] with unit left/right movers
A′ = (cos u, sin u, 0), B′ = (cos v, 0, sin v) — closed, period 2π.
Conformal-gauge identities to machine precision (ẋ·x′ ≤ 4e−17,
ẋ² + x′² − 1 ≤ 2e−16): **exactly conserved, no ends.**

## 2. The measurement: a 14× collapse

Ricci-wave / Riemann-wave of the Green-function field in the wave
zone:

| source | ratio |
|---|---|
| **exact loop** (two probes) | **0.032, 0.034** |
| truncated open string (0047) | 0.46–0.53 |
| conserved binary (0047) | 0.014–0.042 |

The string admixture was the open ends. The string sector now obeys
the same law as the binary: **the Green-function field is vacuum
iff the source is conserved.** The loop radiates healthily
(Riemann wave ~1e−2 at R = 8), as GR loops do.

Two anomaly threads resolve together:

- **0032's "inverted selection rule"** (web traveling waves radiate
  where Nambu–Goto's exactly don't): wrong *source model* — no
  tension, open ends — not wrong field dynamics.
- **Vachaspati's traveling-wave silence** is correctly an
  *infinite-string* statement: a loop cannot carry a traveling-only
  mode, because both movers must close (a constant unit B′ is not
  periodic). The web never disagreed with it; the web's early
  string model just wasn't a string.

## 3. κ is a unit; N is the physics

0048's residue #1 asked for the functional's normalization. The
2+1 identification δ = 8πGm (0012) with the quantum ledger
δ_q = 2π/N, m_q = 1/(4GN) gives

```
8πG × 1/(4GN) = 2π/N     identically, for every G
```

**G cancels from the quantized theory.** It is the
participation→deficit conversion unit — exactly as c is the
tick→length unit (0022) — and the coupling's only physical datum is
**the level N**, which 0029 already classified as the world's
content, not the law. The chain's "one free constant" was a unit
choice plus the rung of the tower.

## Honest limits

- The loop measurement is at one family member, one λ, two probes,
  8 phases; cusp moments (where element velocities → c) are not
  specially treated — the 3% residual likely contains cusp-kernel
  and O(h²) contributions, unseparated.
- §2's "iff" now rests on three source types (binary, open string,
  loop) — still instances, not a theorem; the theorem-shaped
  statement (linearized Bianchi ⇒ vacuum requires ∂T = 0) is
  standard and imported.
- §3 is an algebraic identity plus an interpretive claim (G as
  unit); it does not *derive* N, and the 3+1 version of the
  δ = 8πGm identification inherits 0033's codim ladder rather than
  being re-measured here.

## Open

1. **The lattice Palatini** — now the single remaining constructive
   gap in the chain (0048 residue #2).
2. **The residual separation** (O(v) vs O(h²) in the 1–4%) —
   residue #5, unchanged.
3. **Cusp radiation**: the loop's cusps beam GR's famous bursts;
   the machinery here can measure the web's cusp waveform against
   that structure — the next quantitative correspondence test.
4. **Loop decay**: radiated power of the loop vs GR's Γ·Gμ²
   (Γ ≈ 50–100 for this family) — one Isaacson integration away
   with existing instruments.
