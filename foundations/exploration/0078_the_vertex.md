# 0078 — The vertex: shared frames, emergent simplicity, and the honest non-flip

> **AI-generated, not peer-reviewed.** See [`ATTRIBUTION.md`](../ATTRIBUTION.md) — results here are re-derivations of established work unless explicitly
> marked otherwise, and prior art is credited there.

The wall's last standing stone. At a lattice site six plaquettes
meet, and the frame integral behind the one-plaquette kernel (0072)
must there be done **jointly** — the same four tetrad columns couple
to all six curvatures. That is a 16-dimensional Gaussian integral,
and it closes:

> **W_vertex({F}) = const · Π_k (ε′ + s_k²)^(−1/2)**

with s_k the 16 eigenvalues of the joint coupling matrix S({F_μν})
(blocks (⋆F_μν)/2 between tetrad columns μ and ν). The program's own
vertex amplitude — derived, closed-form, positive pointwise. Code:
`output/0070_the_vertex.py` (8 s).

---

## 1. Anchored

Single-plaquette reduction: the sixteen eigenvalues collapse to
±λ₁/2, ±λ₂/2 (fourfold each) with Σλ² = |F|² and λ₁²λ₂² = Pf² —
0072's kernel invariants recovered. Σ s_k² = Σ|F_p|² on random
six-packs to 10⁻⁶. A seeded 16-dimensional Monte-Carlo bridge
confirms the Gaussian integral on a generic configuration (within
5%).

## 2. Cross-simplicity emerges

Six plaquettes, each **individually simple**, matched norms:

- from a **common tetrad**: price ≈ 14–15 nats
- **unrelated simples**: price ≈ 22–23 nats
- mean extra price of incompatibility: **+7.4 nats**
- while the one-plaquette **product is exactly blind** to the
  difference (per-plaquette prices equal to 10⁻⁶).

The shared-frame integral enforces the off-diagonal Plebanski
constraints — cross-simplicity and closure — that no per-plaquette
weight can see. The constraint structure the spin-foam program
imposes at the vertex by hand is here *generated by the same frame
integral that generated the kernel*.

## 3. The insertion ladder — the suppression 0075 ordered

Replace one plaquette of a tetrad-compatible six-pack:

| insert | in context | isolated |
|---|---|---|
| geometric (its own) | +0.00 | +0.00 |
| foreign simple | **+4.16** | +0.00 |
| non-simple | **+4.88** | +1.87 |

Context amplifies the constraint tier ~3× and charges *compatibility
itself* ~4–5 nats. At the vertex, simple-but-foreign costs nearly as
much as non-simple; **cheap means geometric together.** This is the
vertex-level suppression whose absence 0075 measured at the bare
chain — the (1,0) interleaving's job description, delivered by the
joint frame integral.

## 4. The honest non-flip

Weyl-part vs Ricci-part curvature at matched norm: **Ricci is still
cheaper in the large majority of trials** (Weyl cheaper in 2/24 — no
systematic reversal). The vertex does not make the measure select
vacuum, and 0061 §3's lesson survives exactly where it should: **a
measure is not an equation of motion.**

What the vertex *does* establish is the operational sense of "the
graviton rides": the measure concentrates on **tetrad-geometric**
curvature, so the gapless (1,1) carrier's favored content is metric
fluctuation. Which metric configurations dominate — the vacuum
question — remains the action's job, as the classical arc
(0046/0050) always said.

## The wall arc, closed as planned

Six stones, every one off the derived measure, no knob turned:

1. **0071**: the abelian phase map — D ≤ 3 bleeds, rigidity begins at
   D = 4, N ≥ 3 derived.
2. **0072**: the continuum kernel in closed form — BC's delta plus a
   derived tail.
3. **0073**: the sign problem, met on schedule.
4. **0074**: the cure was the program's own Born rule; the ledger was
   |amplitude|² all along.
5. **0075/0076/0077**: physical spectrum with the graviton leading
   the simple tower; 3D annihilation vs 4D criticality; the fixed
   point is the heat kernel with a gapless graviton channel.
6. **0078**: the vertex generates cross-simplicity and charges
   incompatibility; vacuum selection stays with the action, honestly.

What remains beyond the arc: the true 4D momentum correlator with
intertwiner refinement (the full spin-foam-grade computation), the
Lorentzian lift, and the standing queue (Λ1, C1, the
arithmetic-branch pass, the sign-problem toy, matter).

## Honest limits

- The vertex here is the joint frame integral at one site with
  Gaussian-regulated ℝ⁴ frames — not yet a boundary-state 4-simplex
  amplitude with intertwiner labels; the two should agree in
  content, not proven.
- ε′ fixed at 0.01; amplitude scales chosen for O(10)-nat prices;
  trial counts 8–24, seeded.
- The Weyl/Ricci comparison uses the six-columns lift (0061 §3's
  modelling choice) at matched Frobenius norm.
- Euclidean throughout.

## Open

1. The boundary-state vertex (intertwiners proper) and its
   propagator — the spin-foam-grade completion of A3.
2. The in-context tension spectrum: redo 0075's chain with
   vertex-coupled (shared-frame) links — does the (1,0) mode's
   lightness survive context?
3. Standing: Λ1, C1, arithmetic-branch pass, sign-problem toy,
   the CLT fixed-point theorem, the τ beta function.
